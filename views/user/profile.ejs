<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Info</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"

  />
  <link rel="stylesheet" href="/userSignup.css">
  <link rel="stylesheet" href="/userBrand.css">
  <link rel="stylesheet" href="/sidebar.css">
    <style>
       
        .search input{
      width: 400px;
      height: 40px;
      margin-top: 27px;
      margin-left: 50px;
      border:0.3px solid grey;
      border-radius: 200px;
      padding-left: 50px;
      position: relative;
  }
  .search{
      display: flex;
      
  }
  .search i{
      position: absolute;
      margin-top: 35px;
      margin-left: 75px;
      color: #666;
  }
  .small{
      display: flex;
      
      width: 130px;
      justify-content: space-between;
      margin-left: 70px;
  }
  .small a{
      font-size: x-large;
      color: purple;
      margin-top: 30px;
  }
  .main-div{
    margin-top: 10px;
  }
  
    </style>
</head>
<body>

     <!-- Header -->
   <%- include('../partials/nav.ejs') %>
   <!-- navbar -->
   <div class="box">
     <div class="item">
       <a href="/">Home</a>
       <a href="/category">Category</a>
       <a href="/brand">Brand</a>
       <a href="">About us</a>
       <a href="">Contact us</a>
     </div>
     <%- include('../partials/header.ejs') %>
   </div>
    <div class="container-fluid ">
        <div class="row">
            <!-- Sidebar -->
            <%- include('../partials/sidebar.ejs') %>

            <!-- Main Content -->
            <div class="col-md-9 col-lg-10 p-4 main-div">
                <h2 class="mb-4">Personal info</h2>
                
                <div class="card mb-4">
                    <div class="card-body">
                        <h5 class="card-title mb-4">Basic info</h5>
                        <form action="/profile" method="post" id="profileSubmit">
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label class="form-label">First name</label>
                                    <input type="text" class="form-control" value="<%= (user.fullName?.split(" ")[0] || "") %>" name="firstname">
                                </div>
                                <div class="col-md-6">
              
                                    <label class="form-label">Last name</label>
                                    <input type="text" class="form-control" value="<%= (user.fullName?.split(" ")[1] || "") %>" name="lastname">
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Date of birth</label>
                                <input type="date" class="form-control" placeholder="YYYY-MM-DD" value="<%= user.DOB ? new Date(user.DOB).toISOString().split('T')[0] : '' %>" name="dob">
                            </div>
                            <div class="mt-4">
                                <button type="submit" class="btn btn-primary me-2">Save changes</button>
                                <a href="/"><button type="button" class="btn btn-light">Close</button></a>
                                
                            </div>
                        </form>
                    </div>
                </div>

                <div class="card mb-4">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h5 class="card-title mb-0">Contact</h5>
                           
                        </div>
                        <div class="mb-3">
                            <div><%=(user.fullName || "") %></div>
                            <div>Email: <%= user.email %><span class="verified-badge ms-2">Verified</span></div>
                            
                                
                                
                            
                        </div>
                    </div>
                </div>

                <div class="card mb-4">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h5 class="card-title mb-0">Password</h5>
                            <a href="/changePassword" class="text-decoration-none">Edit</a>
                        </div>
                        <div>••••••••••••••</div>
                    </div>
                </div>

                <div class="mt-5">
                    <h5 class="mb-3">Delete account</h5>
                    <p class="text-muted">When you delete your account, your public profile will be deactivated immediately. If you change your mind before the 14 days are up, sign in with your email and password, and we'll send you a link to reactivate your account.</p>
                    <a href="/deleteUser"><button class="btn btn-outline-danger">Delete account</button></a>
                </div>
            </div>
        </div>
    </div>
    <%- include('../partials/footer') %>
    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>